<ul class="nav py-3 bg-dark mb-3" style="justify-content: space-between">
  <div class="d-flex">
    <li class="nav-item">
      <%= link_to 'Home', root_path, class: "nav-link text-white" %>
    </li>

    <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to 'Department', departments_path, class: "nav-link text-white" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Project', projects_path, class: "nav-link text-white" %>
      </li>
      <li class="nav-item">
        <%= link_to 'List User', user_index_path, class: "nav-link text-white" %>
      </li>

      <li class="dropdown">
        <a href="#" class="dropdown-toggle nav-link text-white" data-toggle="dropdown"> Account <b class="caret"></b>
        </a>
        <ul class="dropdown-menu pl-3 px-3" style="width: 200px">
          <li><%= link_to "Update Info", edit_info_user_path(current_user), class: "btn btn-secondary btn_dropdown" %></li>
          <li><%= link_to "Change Password", edit_user_registration_path(current_user), class: "btn btn-secondary btn_dropdown" %></li>
          <li>
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "nav-link btn btn-secondary btn_dropdown" %>
          </li>
        </ul>
      </div>
      <div class="d-flex" style="margin-right: 60px">
        <div class="locale-select">
          <button class='btn btn-secondary <%= I18n.locale == :vi ? 'active' : '' %>'>
            <%= link_to "VI", locale: "vi" %>
          </button>
          <button class='btn btn-secondary <%= I18n.locale == :en ? 'active' : '' %>'>
            <%= link_to "EN", locale: "en", class: "btn" %>
          </button>
        </div>
        <div>
          <strong style="font-size: 18px; color: white"><%= current_user.name %></strong>
          <br>
          <span style="font-size: 15px; color: white"> <%= current_user.role %></span>
        </div>

      </div>
    <% end %>

    <% unless user_signed_in? %>
      <!--    <li class="nav-item">-->
      <%#= link_to 'Sign Up', new_user_registration_path, class: "nav-link text-white" %>
      <!--    </li>-->

      <li class="nav-item">
        <%= link_to 'LogIn', new_user_session_path, class: "nav-link text-white" %>
      </li>
    <% end %>
    </ul>
